# KB digest — 2026-08-05 (since 2026-08-04)

## What happened

A genuinely quiet window — **no new sources, no new pages.** The only substantive activity was a maintenance lint; both scheduled research runs came back empty (the backlog was already cleared by the 07-31 and 08-03 live-Chrome burn-downs).

- **Lint (08-04):** a Dannie-directed frontmatter pass — **15 `sources:` slug fixes across 13 pages** (consumer pages now consistently cite source-page slugs, not raw-file slugs), and the **EM×Wardley transcript gap was intentionally retired** (Daniel's series is YouTube-only, captions won't load even in live logged-in Chrome — the video-observations note is treated as sufficient). Structure checks clean: 262 pages, 0 broken wikilinks, 0 orphans, index coverage exact.
- **Research (08-04 + 08-05, both headless):** **0 filed** each. Everything in-window was already captured or off-thread. The only in-window new item seen was Willison's *LLM 0.32* release post (08-04) — a tooling changelog that restates already-captured agentic-loop positions; flagged, not captured.
- **No un-ingested raw sources** are sitting in `raw/` — every filed capture is already a wiki page.

## Read next

Nothing landed this window, so rather than pad the list, here are the highest-value **in-house artifacts** worth reading on a quiet day — they're the KB's own answer to its biggest open gap, and a digest rarely surfaces them. All three predate this window (flagged as such); read if you haven't.

1. **Worked Event Model — An Autonomous Coding Factory** · `outputs/worked-event-model-autonomous-coding-factory.md` (+ `.mermaid`) · *in-house deliverable, not new this window (focus — flagged important)*
   **TLDR:** The KB's standing gap made concrete — a *worked* Event Model of a multi-agent/harness system using Event Modeling **the method**, not just event sourcing. It models an autonomous coding factory turning a human feature request into a merged PR: 7 vertical slices, 6 agent processors on role/capability swimlanes, GWT contracts per slice, a guardian-veto subscriber, an autonomy-ladder orchestrator, and a DCB-style per-slice append invariant. Honest status baked in: internally consistent with captured sources, but an illustration — not built or validated against a running system.
   **Why it's worth reading:** This is the artifact that closes "we have no worked example at all" on `event-modeled-agent-design` — the exact focus intersection (EM × agents × harness). If you're going to argue the EM-governs-the-factory thesis at ChronosHub, this is your reference model.

2. **Worked Event Model — An Autonomous Customer-Support Desk** · `outputs/worked-event-model-customer-support-desk.md` · *in-house deliverable, not new this window (focus)*
   **TLDR:** A second worked model in a different domain (the KB's most-cited production use case, customer-support agents) to show the method generalizes. Same agent-as-user / agent-as-processor mapping, same "agents subscribe→reason→publish, never call each other directly" discipline from Confluent/Solace: triage → retrieve context → draft → policy-gated side-effect (refund/plan change) → send → learn, escalating to a human only when policy or confidence demands.
   **Why it's worth reading:** It's the generalization check — proof the coding-factory model wasn't a one-domain fluke. Useful if you want a non-engineering example to put the method in front of non-technical stakeholders.

3. **Denoting an Agent in an Event Model** · `outputs/denoting-an-agent-in-an-event-model.md` · *in-house notation primer, not new this window (focus)*
   **TLDR:** The short notation companion under both worked models — how to actually *draw* an agent on an Event Modeling timeline, resolving Dymitruk's "an agent is a user or an Automation processor" claim into concrete swimlane conventions (when the agent is an actor issuing commands vs. an automation reacting to events, and how the human veto/escalation appears).
   **Why it's worth reading:** It's the missing "how do I notate this" bridge between the theory (`event-modeled-agent-design`) and the two worked models — a 5-minute read that makes the diagrams legible.

*(For the canonical write-side spine — `[[command-context-consistency]]` — see yesterday's digest; it was the lead pick on 08-04 and hasn't changed since.)*

## Do next

- [ ] **Run a live-Chrome social sweep** — the one genuinely actionable research gap. Headless runs are blind to Rico Fritzsche's client-rendered Ghost blog and to login-walled X/LinkedIn feeds (Dilger/Miller/Tornhill/Nick Tune/Goeleven/Fraktalio); the last live sweep was **08-03**, and every headless run since has flagged STEP 1b as pending. → reply *"run a live-Chrome sweep"*.
- [ ] **Chase the DORA primary** — the gated Google Cloud PDF would supersede the InfoQ secondary now in `raw/`, and the **2025 DORA State-of-AI report** is the newly-named want. → reply *"chase the DORA primary"*.
- [ ] **Read the coding-factory worked model** — the KB's own answer to its biggest evidence gap; if it holds up to you, it's the artifact to circulate internally. → reply *"open the coding-factory model"*.
- [ ] **Nothing is waiting to be ingested** — `raw/` has no un-processed captures, so there's no ingest go/no-go pending. *(Status, not an action.)*

## Open threads

- **The worked multi-agent Event Model is still genuinely unpublished** — no independent, published end-to-end multi-agent Event Model exists on the open web; the two in-house artifacts above remain the only ones. Still the biggest evidence gap under `event-modeled-agent-design`.
- **"The Munich Event" foundation + ESDM/EmLang file-format** — watch whether the neutral EM/ES standards proposal and Roden's file-format-plus-linter ambition actually ship; nothing new since the 07-31 recap capture.
- **Fritzsche's blog is client-rendered (Ghost)** — headless watches see only a shell, so treat any headless "Fritzsche evergreen" report as unreliable; his posts only surface via live-Chrome sweeps.
